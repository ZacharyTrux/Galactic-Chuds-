using UnityEngine;

public class GalacticChudInputs : MonoBehaviour
{
    public static GalacticChudInputs()
    {
        
    }
    // Start is called once before the first execution of Update after the MonoBehaviour is created
    private void Awake()
    {
        Instance = this;
        var inputActions = name 
    }
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
